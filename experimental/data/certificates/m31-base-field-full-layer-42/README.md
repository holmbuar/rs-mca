# M31 base-field full-layer 42 certificate

This directory contains the frozen expected output for
`experimental/scripts/verify_m31_base_field_full_layer_42.py`.

The verifier checks exact finite arithmetic for the conditional prime-field
layer theorem.  It is not a quartic-field list certificate, a collision
certificate, a payment, or an official theorem.

Run the four commands in
`experimental/notes/thresholds/m31_base_field_full_layer_42.md`.


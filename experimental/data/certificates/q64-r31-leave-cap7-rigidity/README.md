# q64 leave and fixed-27 cap-seven certificate

This package contains two composable finite Grand List payments:

1. the mandatory 30-shadow leave sharpens the q64-footprint-31 cap by
   `1,493,353,005,097,095`;
2. the fixed-27 contracted-tail theorem proves unconditional multiplicity at
   most seven and classifies the only remaining seven-tail overlap profiles.

The sharpened ledger would close under a uniform cap six, with exact margin
`66,294,825,062,529`.  Cap seven is already too weak, so this package does not
close the one-row theorem or move the official score.

Replay from the repository root:

```text
ruby --disable-gems -w experimental/scripts/verify_q64_r31_mandatory_leave_payment.rb
ruby --disable-gems -w experimental/scripts/verify_hostile_audit_q64_r31_mandatory_leave_payment.rb
ruby --disable-gems -w experimental/scripts/verify_rtail_fixed27_support_cap7_seven_tail_rigidity.rb
```

import Lake
open Lake DSL

package «nonfiber_decomposition_realized_scale» where

@[default_target]
lean_lib «NonfiberDecompositionRealizedScale» where
  roots := #[`NonfiberDecompositionRealizedScale]

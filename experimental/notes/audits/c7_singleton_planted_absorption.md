# Singleton planted profiles and C7 atlas-order noncanonicity

**Status:** `COUNTEREXAMPLE / ACTIVE-ATLAS NONCANONICITY / PROVED FINITE REGRESSION / POLICY DECISION REQUIRED`

## Claim and current interpretation

The archived broad atlas grammar in
`archived/asymptotic_rs_mca_frontiers.tex` admitted a predetermined common
support divisor `P` and treated sublinear planted factors as a subexponential
profile cost. Under that permissive historical reading, a fixed catalogue of
singleton roots

```text
P_t(X) = X - t,  t in D,
```

covers every positive-agreement witness before C7: each nonempty support
contains at least one `t`. Ordered first match can therefore assign all supplied
numeric slopes to those earlier candidate cells and leave the later raw C7 list
empty.

This is a regression against an overbroad atlas grammar, not a theorem selecting
an active policy. The archived grammar is not the active proof authority, and
`AsymptoticSpine.SemanticAtlasOwnership` now supplies only labeled numeric
payments. Its labels do not prove a row-derived factor, actual RS slope,
semantic owner predicate, or least-owner theorem.

Source-level options remain open: restrict planted cells to positive-density
blocks, require named row-derived factors, exclude singleton roots from a
canonical atlas, order C7 earlier, or accept a separately proved earlier owner.
This packet chooses none of them.

## Finite Lean regression

`AsymptoticSpine.C7SingletonPlantedAbsorption` uses four finite records:

```text
(10,{0,1}), (11,{1,2}), (12,{2,3}), (13,{3,0}).
```

The four raw singleton-root numeric cells are

```text
[10,13], [10,11], [11,12], [12,13].
```

Appending the raw C7 cell `[10,11,12,13]` and taking ordered first match gives

```text
[10,13], [11], [12], [], [].
```

The final empty leaf is the assigned C7 list under that explicitly supplied
order.

The module proves:

1. `singletonPlantedRawSlopeCells_eq`, the exact four candidate cells;
2. `singletonPlanted_absorbs_rawC7`, the exact ordered first-match leaves;
3. `singletonPlantedC3Line_totals`, with natural total `3` and direct budget
   `12` in the finite compiler-loss-`4` fixture;
4. `singletonPlantedRawC7_breaks_firstMatchOwnership`, showing that the
   untrimmed raw C7 payment duplicates all four numeric values;
5. `noClosedLineLedger_with_singletonPlanted_then_rawC7`, excluding that
   duplicated profile list from `ClosedLineLedger`;
6. `singletonPlantedC3Ledger_compiles`, replaying the explicitly supplied order
   through the line-local numeric compiler.

The records, values, labels, `badCount`, and finite line list are fixture data.
Lean does not prove that they arise from an actual received RS line.

## What the regression establishes

The deletion-aware C7 adapter pays the literal post-earlier list and permits
that list to be empty. What fails is the atlas-independent inference

```text
raw C7 payment exists
  => C7 must be a nonempty first-match owner independently of atlas order.
```

Conversely, the finite fixture does not authorize singleton-root C3 owners in
an active atlas. `LabeledC3Payment` records only metadata and a numeric payment;
all semantic classification and completeness obligations remain external.

## Historical asymptotic payment calculation

For provenance only, the archived discussion used

```text
D = F_q^×,
n = q - 1,
number of singleton candidate profiles = n,
per-profile direct distinct-slope bound <= q.
```

When `log q = o(n)`, those two factors are subexponential. This explains why the
old permissive grammar created a real policy ambiguity rather than merely a
finite-list curiosity. It is not promoted here as an active C3 theorem.

## Proof census

`C7SingletonPlantedAbsorption.lean` and
`SemanticAtlasOwnership.lean` contain:

```text
sorry / sorryAx placeholders: 0
custom axiom declarations:     0
Mathlib imports:               0
```

The modules print axiom dependencies for their load-bearing compiler theorems.
The exact-head fork Lean check is authoritative for compilation.

## Nonclaims

- No singleton-root profile is proved to belong to an active semantic atlas.
- No concrete RS semantic C3 or C7 owner is proved.
- No global C1--C9 atlas or completeness over all received lines is proved.
- No C7 survivor nonemptiness or universal C7 emptiness is proved.
- No actual row-wide `(UNIF)`, target comparison, adjacent safe row, threshold
  improvement, prize result, or row closure is established.
- No active TeX proof authority is modified.

## Required downstream decision

A later source-level packet must explicitly choose and prove an atlas policy,
then supply its actual bad-slope coverage, least-owner/deletion theorem, profile
count, and line-local payment. Whichever policy is chosen, deletion and
`sum_profile` must remain inside one received line before the outer supremum.
